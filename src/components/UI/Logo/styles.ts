import styled from 'styled-components';

export const LogoContainer = styled.div`
   display: flex;
   align-items: baseline;
   gap: 0.5rem;
   cursor: pointer;
   position: fixed;
   left: 0;
   padding-left: 5vh;
`